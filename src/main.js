const switcher = document.getElementById("switch")
const circle = document.getElementById("circle")

switcher.addEventListener("click", () => {
    // circle.classList.toggle("switch-on")
    switcher.classList.toggle("active")
})